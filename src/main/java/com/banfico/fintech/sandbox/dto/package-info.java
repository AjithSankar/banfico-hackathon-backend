/**
 * Faithful OBIE request/response records (ObieAccount, ObieBalance, ObieTransaction, etc.)
 * matching the exact JSON shapes from the sandbox — no simplification at this layer.
 */
package com.banfico.fintech.sandbox.dto;
