/**
 * Sandbox integration layer (Phase 2, blocking for all other phases): SandboxTokenService
 * (Keycloak token exchange/refresh/cache) and SandboxAisClient (typed OBIE endpoint wrapper).
 * Raw OBIE request/response records live in {@code sandbox.dto}.
 */
package com.banfico.fintech.sandbox;
