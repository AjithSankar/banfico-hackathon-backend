/**
 * Spring AI layer (Phase 6): ChatClient config, FinancialAssistantService, tool/function
 * definitions grounded in live sandbox data, and ChatController. Uses the local Ollama
 * provider configured in application.yaml.
 */
package com.banfico.fintech.ai;
